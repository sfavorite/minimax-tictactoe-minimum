# Minimax Algorithm - Tic Tac tic-tac-toe

This is a basic implementation of tic-tac-toe using the minimax algorithm for the
computers move.

You can compile this with gcc (or clang):

```c
gcc -o tictactoe main.

```
